{
  "name": "wa-synapse",
  "version": "0.1.0",
  "private": true,
  "description": "A modular, plugin-based WhatsApp bot framework built on whatsapp-web.js",
  "scripts": {
    "build": "pnpm --filter @wa-synapse/core build",
    "start": "pnpm --filter wa-synapse-example start",
    "example:dev": "pnpm --filter wa-synapse-example dev",
    "postinstall": "pnpm --filter @wa-synapse/core build"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
